---
description: Co-locate types with their modules
globs: ["**/*.ts", "**/*.tsx"]
alwaysApply: true
---

# Co-locate Types

Keep types, interfaces, and schemas in the same directory as the code that uses them.
Do NOT create a global `types/` directory.